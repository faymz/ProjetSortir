{{ form_start(form) }}
    <div>{{ form_row(form.nom) }}</div>
    <div>{{ form_row(form.dateHeureDebut) }}</div>
    <div>{{ form_row(form.dateLimiteInscription) }}</div>
    <div>{{ form_row(form.nbInscriptionsMax) }}</div>
    <div>{{ form_row(form.duree) }}</div>
    <div>{{ form_row(form.infosSortie) }}</div>
    <div>
        <Label>Campus : </Label>
        {{ sortie.organisateur.campus.nom }}
    </div>
    <div>
        {{form_row(form.ville) }}
    </div>
    <div id="creerLieu">
        {{ form_row(form.lieu) }} <a type="button" class="btn btn-light" href="{{ path('app_lieu_new') }}"><b>+</b></a>
    </div>
    <div>
        {{ form_row(form.rue) }}
    </div>
    <div>
        {{ form_row(form.codePostal) }}
    </div>
    <div>
        {{ form_row(form.latitude) }}
    </div>
    <div>
        {{ form_row(form.longitude) }}
    </div>


    <button class="btn btn-primary" >{{ button_label|default('Enregistrer') }}</button>
    <!--TODO modifier la redirection du bouton publier la sortie -->
    <button class="btn btn-primary" onclick="document.location='{{ path('app_sortie_index') }}'">{{ button_label|default('Publier la sortie') }}</button>
{{ form_end(form) }}
    <div id="btnCreer">
        <button class="btn btn-primary" onclick="document.location='{{ path('app_sortie_index') }}'">{{ button_label|default('Retour à la liste sorties') }}</button>
    </div>
{% block javascripts %}
    <script src="{{ asset('js/SelectVille.js') }}"></script>
{% endblock %}