{{ form_start(form) }}
    {{ form_widget(form) }}
    <div>
        <Label>Campus : </Label>
        <label>{{ sortie.organisateur.campus.nom }}</label>
    </div>
    <button class="btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
