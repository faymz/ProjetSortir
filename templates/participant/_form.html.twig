{{ form_start(form) }}
{% if app.user %}
    <div>{{ form_row(form.pseudo) }}</div>
    <div>{{ form_row(form.prenom) }}</div>
    <div>{{ form_row(form.nom) }}</div>
    <div>{{ form_row(form.telephone) }}</div>
    <div>{{ form_row(form.email) }}</div>
    <div>{{ form_row(form.campus) }}</div>
    <div>{{ form_row(form.image) }}</div>
{% else %}
    {{ form_widget(form) }}
{% endif %}
    <div>
        <button class="btn btn-primary" >Enregistrée</button>
    </div>
    {{ form_end(form) }}

